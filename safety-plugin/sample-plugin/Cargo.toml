[package]
name = "safety-plugin-sample"
version = "0.1.0"
edition = "2021"

[lib]
# cdylib: .soファイルとして出力する（ホストがdynamic loadする）
crate-type = ["cdylib"]

[dependencies]
abi_stable = "0.11"
safety-plugin-common = { path = "../common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
